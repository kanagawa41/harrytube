<h1><%= t('views.posts.regist_title') %></h1>

<%= render 'form', post: @post %>

<br>

<%= link_to t('views.posts.show_list'), posts_user_infos_path(current_user.user_info.hash_id), class: "btn btn-link" %>