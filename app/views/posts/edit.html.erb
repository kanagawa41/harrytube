<div class="btn-toolbar">
  <div class="btn-group">
    <%= link_to t('views.posts.show_post_info_btn'), @post, class: "btn btn-default" %>
  </div>
</div>

<h1><%= t('views.posts.edit_title') %></h1>

<%= render 'form', post: @post %>